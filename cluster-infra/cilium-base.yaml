# cilium-base-values.yaml
ipam:
  mode: "kubernetes"
operator:
  replicas: 1
kubeProxyReplacement: true
k8sServicePort: 8443
envoy:
  enabled: true
gatewayAPI:
  enabled: true
clustermesh:
  useAPIServer: true
  apiserver:
    service:
      type: NodePort